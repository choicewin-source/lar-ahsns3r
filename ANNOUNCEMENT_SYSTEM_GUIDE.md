# دليل نظام الإعلانات 📢

## نظرة عامة
تم إنشاء نظام إعلانات كامل يسمح للمدير بإضافة إعلانات تظهر في شريط متحرك أعلى الصفحة الرئيسية.

---

## المميزات ✨

1. **إضافة إعلانات** بسهولة من لوحة تحكم المدير
2. **شريط متحرك** جذاب يعرض الإعلانات بشكل تلقائي
3. **تفعيل/إيقاف** الإعلانات بنقرة واحدة
4. **حذف** الإعلانات القديمة
5. **تصميم متجاوب** يعمل على جميع الأجهزة
6. **رسوم متحركة** سلسة وجذابة

---

## كيفية الاستخدام

### للمدير (إضافة إعلان جديد):

1. **سجل دخول كمدير**
   ```
   البريد: admin@ahsansanar.com (أو حسابك كمدير)
   ```

2. **اذهب إلى لوحة التحكم**
   ```
   http://localhost:8000/admin/dashboard
   ```

3. **اضغط على زر "إدارة الإعلانات"**
   - ستجده في أعلى الصفحة بجانب "مدير النظام"
   - أو اذهب مباشرة إلى: `http://localhost:8000/admin/announcements`

4. **أضف إعلان جديد**
   - املأ حقل "نص الإعلان"
   - مثال: `عروض خاصة على جميع الأجهزة الكهربائية حتى نهاية الشهر! 🎉`
   - اضغط "إضافة الإعلان"

5. **الإعلان سيظهر فوراً** في الشريط المتحرك أعلى الصفحة الرئيسية!

---

## إدارة الإعلانات

### تفعيل/إيقاف إعلان:
- اضغط على أيقونة 🟢 لإيقاف الإعلان
- اضغط على أيقونة ⚫ لتفعيل الإعلان
- **فقط الإعلانات المفعلة** تظهر في الشريط

### حذف إعلان:
- اضغط على أيقونة سلة المهملات 🗑️
- أكد الحذف
- سيتم حذف الإعلان نهائياً

---

## أمثلة على إعلانات جيدة

### ✅ أمثلة موصى بها:

```
✨ عروض حصرية على الهواتف الذكية - خصم يصل إلى 30%!

🎉 افتتاح فرعنا الجديد في خانيونس - زوروا الآن!

📢 توفر الآن: أحدث أجهزة اللابتوب بأسعار منافسة

🔥 عرض محدود: اشترِ 2 واحصل على الثالث مجاناً!

⚡ شحن مجاني لجميع الطلبات فوق 500 شيكل!
```

### ❌ أمثلة يجب تجنبها:

```
❌ "اتصل على 0591234567 الآن للحصول على عرض خاص..." (طويل جداً)
❌ "منتج جديد" (غير واضح)
❌ إعلان بدون رموز تعبيرية (ممل)
```

---

## نصائح لكتابة إعلان فعال

1. **اجعله قصيراً** - أقل من 100 حرف أفضل
2. **استخدم رموز تعبيرية** - 🎉 ⚡ 🔥 ✨ 
3. **ركز على فائدة واحدة** - عرض، خصم، خدمة جديدة
4. **كن واضحاً** - اذكر التفاصيل الأساسية
5. **أضف إحساساً بالإلحاح** - "محدود"، "حتى نهاية الشهر"

---

## التصميم والألوان

### الشريط المتحرك:
- **اللون**: أحمر متدرج (يتناسب مع هوية الموقع)
- **الحركة**: تمرير تلقائي من اليمين لليسار
- **السرعة**: تتناسب مع عدد الإعلانات
- **تأثيرات**: لمعة متحركة للفت الانتباه

### الأيقونة:
- مكبر صوت 📢 في بداية الشريط
- زر إغلاق ✕ لإخفاء الشريط مؤقتاً

---

## المواقع التي يظهر فيها الشريط

الشريط يظهر في **جميع الصفحات** أسفل الـ Navigation Bar مباشرة:
- ✅ الصفحة الرئيسية
- ✅ صفحات المنتجات
- ✅ صفحة المتاجر
- ✅ لوحة تحكم المستخدمين
- ✅ جميع الصفحات الأخرى

---

## الأسئلة الشائعة

### س: كم عدد الإعلانات التي يمكن إضافتها؟
**ج:** لا يوجد حد! يمكن إضافة عدد غير محدود من الإعلانات، وسيتم عرضها جميعاً في الشريط المتحرك.

### س: هل يمكن إيقاف إعلان مؤقتاً؟
**ج:** نعم! استخدم زر التفعيل/الإيقاف لإيقاف الإعلان مؤقتاً دون حذفه.

### س: هل يمكن ترتيب الإعلانات؟
**ج:** حالياً يتم عرض الإعلانات حسب تاريخ الإضافة (الأحدث أولاً). الإعلانات الجديدة تظهر في البداية.

### س: ماذا يحدث عند عدم وجود إعلانات؟
**ج:** الشريط المتحرك لن يظهر نهائياً إذا لم تكن هناك إعلانات مفعلة.

### س: هل يمكن تعديل إعلان موجود؟
**ج:** حالياً لا. يمكنك حذف الإعلان القديم وإضافة إعلان جديد.

---

## الملفات المضافة/المعدلة

### ملفات جديدة:
- `app/Models/Announcement.php` - موديل الإعلانات
- `app/Http/Controllers/Admin/AnnouncementController.php` - كونترولر إدارة الإعلانات
- `database/migrations/2026_01_04_082043_create_announcements_table.php` - جدول قاعدة البيانات
- `resources/views/admin/announcements/index.blade.php` - صفحة إدارة الإعلانات
- `resources/views/components/announcement-ticker.blade.php` - الشريط المتحرك
- `ANNOUNCEMENT_SYSTEM_GUIDE.md` - هذا الملف

### ملفات معدلة:
- `routes/web.php` - إضافة routes الإعلانات
- `resources/views/layouts/app.blade.php` - إضافة الشريط المتحرك
- `resources/views/admin/dashboard.blade.php` - إضافة زر "إدارة الإعلانات"

---

## قاعدة البيانات

### جدول `announcements`:
```sql
- id (bigint) - المعرف الفريد
- content (text) - نص الإعلان
- is_active (boolean) - هل الإعلان مفعل؟
- created_at (timestamp) - تاريخ الإضافة
- updated_at (timestamp) - تاريخ آخر تحديث
```

---

## الأوامر المفيدة

```bash
# عرض جميع الإعلانات
php artisan tinker
\App\Models\Announcement::all();

# عرض الإعلانات المفعلة فقط
\App\Models\Announcement::active();

# إضافة إعلان من الكونسول
\App\Models\Announcement::create([
    'content' => 'إعلان تجريبي 🎉',
    'is_active' => true
]);

# حذف جميع الإعلانات (حذر!)
\App\Models\Announcement::truncate();
```

---

## التخصيص المتقدم

### تغيير لون الشريط:
افتح `resources/views/components/announcement-ticker.blade.php` وعدل الـ CSS:
```php
<!-- السطر 6 -->
<div class="bg-gradient-to-r from-red-600 via-red-700 to-red-600 ...">
<!-- غير الألوان إلى blue-600, green-600, etc. -->
```

### تغيير سرعة الحركة:
افتح `resources/views/components/announcement-ticker.blade.php`:
```php
<!-- السطر 18 -->
style="animation: ticker-scroll {{ $announcements->count() * 10 }}s ..."
<!-- غير الرقم 10 إلى رقم أكبر للإبطاء أو أصغر للإسراع -->
```

---

## الاختبار

### اختبر النظام:

1. **أضف إعلان:**
   ```
   http://localhost:8000/admin/announcements
   ```

2. **تحقق من ظهوره:**
   ```
   http://localhost:8000/
   ```

3. **جرب التفعيل/الإيقاف**

4. **جرب الحذف**

5. **أضف عدة إعلانات** وشاهد الحركة المستمرة!

---

## التطوير المستقبلي (اختياري)

أفكار لتحسينات مستقبلية:
- [ ] إضافة ألوان مخصصة لكل إعلان
- [ ] إضافة روابط قابلة للنقر في الإعلانات
- [ ] إضافة صور أو أيقونات مخصصة
- [ ] جدولة الإعلانات (تاريخ بداية ونهاية)
- [ ] إحصائيات عدد المشاهدات
- [ ] إمكانية تعديل الإعلانات
- [ ] إمكانية إعادة ترتيب الإعلانات

---

## الدعم

إذا واجهت أي مشكلة:
1. تأكد من تشغيل `php artisan migrate`
2. امسح الكاش: `php artisan cache:clear`
3. تأكد من أنك مسجل دخول كمدير
4. تحقق من ملف `storage/logs/laravel.log`

---

**تم بنجاح!** 🎉

الآن يمكنك إضافة إعلانات وستظهر تلقائياً في الشريط المتحرك!